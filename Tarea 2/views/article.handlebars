<div class="container-article">
  <h1>{{article.title}}</h1>
  <h2>By {{article.author}}</h2>
  <p>{{article.description}}</p>
  <img class="title-img" src="{{article.urlToImage}}" alt="{{article.title}}" />
  <p>{{article.publishedAt}}</p>
</div>